import { AdoptionApplication } from './adoption-application';

describe('AdoptionApplication', () => {
  it('should create an instance', () => {
    expect(new AdoptionApplication()).toBeTruthy();
  });
});
