import { AdvertisementDirective } from './advertisement.directive';

describe('AdvertisementDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvertisementDirective();
    expect(directive).toBeTruthy();
  });
});
